# WangCai 更新日志 (Update Log)

## [2026-08-27] - 本次更新
### 核心架构：UUID 分布式同步方案
- **主键体系变更**：全面迁移至 `String (UUID)` 主键。彻底解决多设备离线记录导致的编号冲突问题。
- **本地主权同步**：数据创建即分配 UUID，同步流程简化为直接 UPSERT，移除复杂的云端 ID 回填逻辑。
- **数据库基准重置**：数据库版本升至 **22**。移除了所有旧版的数字 ID 迁移代码，将 UUID 架构设为新的系统基准。

### 鲁棒性与防重机制
- **并发互斥锁 (Mutex)**：ViewModel 引入 `baseDataMutex`。确保基础数据（食具、药品等）在极短时间内的重复操作不会产生冗余 UUID。
- **直连数据库校验**：CUD 操作直接查询 Room，彻底解决启动竞争导致的“同步跳过”或数值计算偏差问题。
- **自动清理冗余**：启动时自动扫描并合并因旧版逻辑产生的重复“食盆/水盆”记录。

### UI 与交互体验
- **点击自动全选**：所有输入框在获得焦点时自动选中内容，实现“点即替换”。
- **实时时钟校准**：首页及弹窗时间每 5 秒自动同步当前系统时间（仅限当日记录），确保录入精准度。
- **滚轮生日选择**：宠物生日改用 Wheel Picker 选择，交互风格全系统统一。

### 同步日志精简化
- **状态原位更新**：日志与记录 UUID 绑定。补传成功后直接更新原本失败的日志条目，保持同步历史简洁明了。

---

## [2026-08-26] - 上一次更新
### 核心架构：UUID 迁移与同步闭环重构
- **主键体系变更**：将所有业务表主键 `编号` 从 `Long` 迁移至 `String (UUID)`。彻底解决多设备离线操作冲突。
- **本地主权模式**：数据创建即生成 UUID。同步流程简化为：带着 UUID 直接 UPSERT 到云端。
- **数据库升级**：版本升至 **22**，执行破坏性迁移。
- **Excel 导入增强**：适配旧版数字 ID 导入。系统自动探测并转换为 UUID 映射。
- **时间格式标准化**：统一使用 ISO 8601 格式进行云端传输。
