# WangCai 更新日志 (Update Log)

## [2026-08-31] - 本次更新
### UI 与交互体验增强
- **排泄记录视图升级**：将“状况如何”选择从纯文字按钮改为“图标 + 文字”排列，并支持点击图标直接保存。引入了专用的软便、稀便、尿多、尿少视觉素材，提升记录直观性。
- **时间选择器优化**：在时间选择弹窗中新增“此刻”快速校准按钮。点击即可一键将滚轮同步至系统当前时间，极大简化了实时补录的流程。
- **资源文件标准化**：将 `res/drawable` 目录下的所有中文资源文件名统一重命名为英文，增强了项目的跨平台编译兼容性。
- **精简化弹窗逻辑**：移除了时间选择页面的“取消”按钮，减少视觉干扰，保持操作逻辑的一致性（通过点击外部区域取消）。

---

## [2026-08-28] - 上一次更新
### 核心功能与逻辑修复
- **上次总重持久化**：将饮食记录中的 `grossWeight` 字段从临时变量改为持久化存储（DB v23）。解决了导入数据或云端同步后，记录基准总重丢失导致计算错误的问题。
- **浮点数精度优化**：对所有涉及重量的存储与展示增加“四舍五入保留一位小数”处理。彻底消除了 `471.8999939` 这种由于二进制浮点运算产生的长尾噪声。
- **食具智能识别增强**：在导入与同步逻辑中，将水碗记录的识别关键词从仅检查“水”扩展为检查 **“水”** 或 **“喝”**。解决了“喝喝”记录被错误归类为“饮食”的 Bug。

### 构建与性能优化
- **移除启动闪屏**：删除了 `MainActivity` 中强制显示的 0.5 秒 Logo 启动页，App 点击即进入主界面，大幅提升响应速度。
- **R8 (混淆) 兼容性修复**：针对 Apache POI 5.x 在 Release 模式下因 XMLBeans 资源被移除导致的崩溃问题，在 `rules.keep` 中增加了深度保留规则，并优化了 ClassLoader 加载逻辑。
- **数据库迁移安全性**：在迁移脚本中增加了“防御性”列检查，防止因重复执行 `ALTER TABLE` 导致的启动闪退。

### 数据交互完善
- **Excel 模板升级**：导出/导入功能同步支持“上次总重”列，确保本地备份的数据完整性。

---

## [2026-08-27] - 上一次更新
### 核心架构：UUID 迁移与同步闭环重构
- **主键体系变更**：将所有业务表主键 `编号` 从 `Long` 迁移至 `String (UUID)`。彻底解决多设备离线操作冲突。
- **本地主权模式**：数据创建即生成 UUID。同步流程简化为：带着 UUID 直接 UPSERT 到云端。
- **数据库升级**：版本升至 **22**，执行破坏性迁移。
- **Excel 导入增强**：适配旧版数字 ID 导入。系统自动探测并转换为 UUID 映射。
- **时间格式标准化**：统一使用 ISO 8601 格式进行云端传输。
